package com.example.edwinb.agiletrailblazers.Configuration;

/**
 * Simple listener interface for listening to response's from service calls.
 */
public interface ServiceListener {
    void handleResponse(Object response);
}
